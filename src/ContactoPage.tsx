import Formulario from "./components/Formulario"
const ContactoPage = () => {
  return (
    <div>
      <section className="m-auto max-w-[30rem]">
        <h3 className="text-2xl mb-4">Formulario de Contacto</h3>
        <Formulario/>
      </section>
    </div>
  )
}

export default ContactoPage
